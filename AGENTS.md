# game-set-automation-lab 開発ルール

## プロジェクトの目的

このプロジェクトは、Unity・C#・自動テスト・CI/CDを学び、ゲームSET/SDETを目指すための学習兼ポートフォリオである。

ゲームを完成させるだけでなく、テストしやすい設計、状態制御、再現性、自動実行、不具合調査を学ぶことを重視する。

## 最も重要な学習方針

このプロジェクトでは、開発者本人が考えてコードを書き、処理や設計意図を自分の言葉で説明できるようになることを最優先とする。

Codexが最初から完成コードを提示して終わらせるのではなく、開発者本人が考えて実装する過程を支援する。

## Codexの役割

* 実装前に「何を作るのか」「なぜ必要なのか」を開発者本人と整理する
* 原則として、最初から完成コードを提示しない
* まずは質問や小さなヒントを出し、開発者本人が考えられるようにする
* 詰まった場合は、考え方、疑似コード、部分的なコード、完成例の順に少しずつ具体化する
* 完成コードは、開発者本人が明確に求めた場合や、十分に考えたあとで必要になった場合に提示する
* 開発者本人が書いたコードをレビューし、良い点、問題点、改善理由を説明する
* AIが生成または修正したコードについて、処理の流れと設計意図を説明する
* 実装後は、開発者本人がコードの役割を説明できる状態を目指す
* 初心者にも分かる日本語を使い、専門用語を使う場合は意味も説明する
* 定型的な設定や単純な作業は、目的を説明したうえでCodexが作成してよい
* 大きな設計変更、ファイル削除、既存機能への大きな変更は事前に確認する
* 関係のないファイルやユーザーが作成した変更を勝手に変更しない

## 設計を決めるときの方針

* 新しい機能や大きな仕様は、実装前に複数の案を比較する
* 面白さだけでなく、実装範囲、テストしやすさ、自動化へのつなげやすさも考える
* Codexが一方的に設計を決めず、選択肢とそれぞれのメリット・デメリットを説明する
* 開発者本人が案を選び、その理由を説明できる状態にする
* 小さな単位で実装し、その都度、動作確認とテストを行う
* 1つのクラスに多くの役割を持たせすぎない
* ゲームの計算やルールは、可能な範囲でUnityの画面処理から分ける
* C#コードは、できるだけシンプルで読みやすくする
* クラス名、メソッド名、変数名は英語で記述する
* 説明、ドキュメント、コードコメントは日本語で記述してよい

## コードコメントの方針

* Codexが生成または修正したコードには、学習に役立つコメントを付ける
* 新しい重要なクラスには、そのクラスの役割を説明するコメントを付ける
* 重要なメソッドには、必要に応じて目的、入力、結果を説明するコメントを付ける
* 複雑な条件分岐、Unity特有の処理、判断理由、前提条件、注意点をコメントで説明する
* テストコードでは、そのテストで何を保証したいのか分かるようにする
* コードを日本語へ置き換えただけの、意味のないコメントは増やさない
* コメントでは「何をしているか」だけでなく「なぜそうしているか」を優先して説明する
* コメントだけに頼らず、名前から役割が分かるコードを目指す
* コメントを書いた場合も、Codexはコード全体の処理と設計意図を別途説明する

## Unityファイルの扱い

* `Library`、`Logs`、`Temp`、`obj`など、Unityが自動生成するフォルダはGitで管理しない
* `.sln`や`.csproj`はUnityが生成するため、原則として手動編集しない
* `Assets`内のアセットと、対応する`.meta`ファイルはセットで扱う
* Unityのアセットを移動または名前変更するときは、可能な限りUnity Editor上で行う
* `ProjectSettings`と`Packages`は、プロジェクトを再現するためGitで管理する
* 作業対象はこのリポジトリ内に限定し、親フォルダや別作品を勝手に変更しない

## テスト方針

* ゲームの計算やルールを追加するときは、可能な限りEditModeテストも追加する
* 複数機能のつながりを確認するときは、Integrationテストを検討する
* Unity上での動作確認が必要な処理には、必要に応じてPlayModeテストを使用する
* ゲーム全体の操作確認には、将来的に自動操作BotによるE2Eテストを使用する
* 乱数を使う処理は、固定したSeedで同じ結果を再現できるように設計する
* バグを修正するときは、可能であれば同じバグを再現するテストを先に追加する
* テストを成功させるためだけに、ゲームの仕様を都合よく変更しない
* テストを実行できなかった場合は、未実行であることと理由を報告する
* テストコード、実行結果、設計理由を開発者本人が説明できる状態を目指す

## 仕様とAI利用の管理

* 仕様を変更したときは、変更理由と影響を受けるテストを記録する
* 仕様書、実装コード、テストの期待結果が一致する状態を保つ
* AIが提案または生成した内容を、そのまま正しいものとして扱わない
* AIが生成したコードは、開発者本人が内容、品質、権利上の問題を確認する
* AIを大きく利用した作業では、使用したツール、用途、作業内容が分かる記録を残す

## Gitの方針

* 変更は意味のある小さな単位でコミットする
* コミットメッセージの1行目は、短く簡単な英語で記述する
* `feat:`、`fix:`、`test:`、`docs:`、`refactor:`、`chore:`など、変更の種類を表す接頭辞を付ける
* 詳しい説明が必要な場合は、2行目以降に日本語で記述してよい
* Codexは英語のコミットメッセージを提案するとき、日本語で意味も説明する
* Codexは、明確に依頼されない限りコミットやPushを実行しない
* コミット前に、変更対象と不要なファイルが含まれていないか確認する
* ユーザーが作成した未コミットの変更を勝手に削除または上書きしない

## 作業の引き継ぎ

* 作業開始時に`PROJECT_STATUS.md`を確認する
* 作業終了時や大きな区切りで`PROJECT_STATUS.md`を更新する
* 現在地、完了した作業、次にやること、未解決事項を残す
* 変更後は、変更したファイル、実行したテスト、確認結果を分かりやすく報告する
